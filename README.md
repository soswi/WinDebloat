# WinDebloat
WinDebloat – a configuration-driven Windows debloating framework designed to run during OS installation using autounattend.xml. Every change is controlled by explicit toggles with clear explanations and consequences.
